{
  "name": "bulgarian-news-aggregator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run generate:news && vite --host 0.0.0.0",
    "build": "npm run generate:news && tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "generate:news": "node scripts/generate-news.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
